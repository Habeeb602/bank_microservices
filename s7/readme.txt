from s6 -> s7: removed rabbit mq dependency, h2 database, bus refresh

s7 involves all configuration developments including MySQL DB configuration and testing

These are accomplished through MySQL image from docker and docker-compose file to run all the containers.